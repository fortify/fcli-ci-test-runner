name: 'Setup Java Tools'
description: 'Sets up Java (and Maven cache) for Java-based sources'
inputs:
  java-version:
    description: 'Java version to install'
    required: false
    default: '17'
  cache-maven:
    description: 'Enable Maven dependency cache'
    required: false
    default: 'true'
runs:
  using: 'composite'
  steps:
    - name: Setup Java
      uses: actions/setup-java@v5
      with:
        distribution: temurin
        java-version: ${{ inputs.java-version }}
        cache: ${{ inputs.cache-maven == 'true' && 'maven' || '' }}

    - name: Set up Maven
      uses: fortify/3rdparty-actions/actions/stcarolas/setup-maven/v5@main
      with:
        maven-version: 3.9.12

    - name: Verify Java and Maven
      shell: bash
      run: |
        java -version
        mvn -v
